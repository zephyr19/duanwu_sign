import { AppConfig } from "remax/wechat";

const config: AppConfig = {
  pages: ['pages/index/index'],
  window: {
    navigationBarTitleText: '端午签到小程序',
    navigationBarBackgroundColor: '#5683f6'
  }
};

export default config;
